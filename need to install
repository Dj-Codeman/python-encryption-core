pip install wget QtPy pyjq

pacman -sy make jq bison libtool automake autoconf flex unzip python3
~100Mib

python wget as root 
python jq as root


jq
    tar -xvf
    python3 setup build
    doas python3 setup install
    

wget 
    unzip
    doas python3 setup install 